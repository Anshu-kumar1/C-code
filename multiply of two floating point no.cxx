#include<stdio.h>
int main()
{
    float num1,num2,result;
    printf("enter 1st no.");
    scanf("%f",&num1);
     printf("enter 2nd no.");
     scanf("%f",&num2);
    result=num1*num2;
     printf("multiply of two floating-point no. is %f",result);
    return 0;
}